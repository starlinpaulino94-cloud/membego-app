// PASE Digital Platform — DB-001 Database Blueprint
// Schema implementation pending Sprint 1.2
// See: docs/03-ARQUITECTURA/DB-001 Database Blueprint.md

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
